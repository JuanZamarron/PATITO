programa elementomasgrande;
var int i, n;
float arr[100];

principal()
{
    escribe("Enter total number of elements(1 to 100): ");
    lee(n);
    i = 0;
    mientras (i<n) haz
    {
       escribe("Enter Number " , i + 1 , " : ");
       lee(arr[i]);
       i = i + 1;
    }

    i = 0;
    mientras (i<n) haz
    {
       if(arr[0] < arr[i])
       {
        arr[0] = arr[i];
       }
       i = i + 1;
    }

    escribe("Largest element: " , arr[0]);
}